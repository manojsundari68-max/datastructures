//Get a String from Command Line and Print Its Length

#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[])
{
    if (argc != 2)
    {
        printf("Usage: %s <string>\n", argv[0]);
        return 1;
    }

    printf("String: %s\n", argv[1]);
    printf("Length: %lu\n", strlen(argv[1]));

    return 0;
}
